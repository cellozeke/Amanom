import SnackOrderItem from "./snack_order_item"

const mapStateToProps = state => ({

})

const mapDispatchToProps = dispatch => ({

})

const SnackOrderItemContainer = connect(mapStateToProps, mapDispatchToProps)(SnackOrderItem)
export default SnackOrderItemContainer
